/**
 * Client Status Enumeration
 * Defines the different statuses available for MSSP clients
 */
export enum ClientStatus {
  PROSPECT = 'prospect',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  EXPIRED = 'expired',
  RENEWED = 'renewed',
} 