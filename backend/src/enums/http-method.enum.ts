/**
 * HTTP Method Enum
 * Defines the HTTP methods supported for external API calls
 */
export enum HttpMethod {
  GET = 'GET',
  POST = 'POST'
  // PUT, PATCH, DELETE can be added later as needed
} 